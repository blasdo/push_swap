#include "push_swap.h"

int	swap(t_list **stacks)
{
	swap_a(stacks);
	swap_b(stacks);
	return (9);
}
int	rotate(t_list **stacks)
{
	rotate_a(stacks);
	rotate_b(stacks);
	return (10);
}
int	rrotate(t_list **stacks)
{
	rrotate_a(stacks);
	rrotate_b(stacks);
	return (11);
}
